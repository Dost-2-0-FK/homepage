PORT=<port>

# "local" = your laptop using your private mailbox via authenticated SMTP
# "server" = on your server using local Postfix (DKIM, SPF/DMARC by your domain)
MODE=<local|server>

# Local mode (your private mailbox)
LOCAL_FROM=<your@mail.domain>
LOCAL_SMTP_HOST=<smtp.mail.domain>
LOCAL_SMTP_PORT=<smtp-port>
LOCAL_SMTP_USER=<your@mail.domain>
LOCAL_SMTP_PASS=<password>
LOCAL_SMTP_SECURITY=<starttls|ssl>   

# Server mode (use server's Postfix)
SERVER_FROM=<your@mail.domain>
